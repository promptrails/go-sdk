package promptrails

// Version is the semantic version of this SDK release. Keep in sync with
// the git tag — the module proxy uses the tag, this constant is for
// client-side telemetry (User-Agent) and debug logging.
const Version = "0.4.0"
